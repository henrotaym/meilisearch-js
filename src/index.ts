import MeiliSearchFilters from "./filters";
import MeiliSearchClient from "./client";

const defaults = { Filters: MeiliSearchFilters, Client: MeiliSearchClient };

export { MeiliSearchFilters, MeiliSearchClient };
export default defaults;
