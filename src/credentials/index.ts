import Credential from "./Credential";
import JsonCredential from "./JsonCredential";

const credentials = { Credential, JsonCredential };

export { Credential, JsonCredential };

export default credentials;
